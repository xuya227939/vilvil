import { create } from 'zustand'
import { persist } from 'zustand/middleware'

// export const contentStore = create((...a) => ({
//   ...createContent(...a),
// }))

// export { createPopupStore } from '~store/popup/store';
